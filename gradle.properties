# Machine-specific settings (JVM heap, the Windows trust store for a re-signing
# proxy, and the proxy itself) are kept in the Gradle user home file
# (~/.gradle/gradle.properties), which is not committed. This file stays portable.
org.gradle.parallel=true
