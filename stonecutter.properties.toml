# Centralized version properties (see https://stonecutter.kikugie.dev/wiki/config/properties).
mod.id = "waypointmenu"
mod.name = "Waypoint Menu"
mod.group = "com.waypointmenu"
mod.version = "1.1.0"

# Fabric Loader is version-agnostic across the target range; one version for all.
deps.fabric_loader = "0.19.3"

# 26.x uses Mojang official mappings (Mojmap) — Yarn is discontinued after 1.21.11,
# so there is no deps.yarn here.

["26.1"]
mod.mc_compat = ">=26.1 <26.2"
mod.mc_range = "26.1"
deps.fabric_api = "0.145.1+26.1"
deps.modmenu = "18.0.0"

["26.2"]
mod.mc_compat = ">=26.2 <26.3"
mod.mc_range = "26.2"
deps.fabric_api = "0.159.0+26.2"
deps.modmenu = "20.0.1"
